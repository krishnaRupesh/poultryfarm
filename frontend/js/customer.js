document.getElementById('customer-form').addEventListener('submit', function(event) {
    event.preventDefault();
    alert('Customer added successfully!');
    // Add logic to save customer to database
});
