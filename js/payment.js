document.getElementById('payment-form').addEventListener('submit', function(event) {
    event.preventDefault();
    alert('Payment added successfully!');
    // Add logic to save payment to database
});
